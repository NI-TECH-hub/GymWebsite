touch. gitignore
 git config --global user.email "nikhilchinchore2002@gmail.com"
 git config --global user.name "NI-TECH-hub"

git init
git add .
git commit -m "Initial Commit"
git status #just to see status
ssh-keygen -t ed25519 -C "nikhilchinchore2002@gmail.c
Generating public/private ed25519 key pair.

#add this key to yout Github account

git remote add origin <ssh url.git>
git push origin master